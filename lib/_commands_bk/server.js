var app = require('../divshot');
// require('superstatic-server');

module.exports = function () {
  console.log('Booting server...'.yellow);
  console.error('no server yet D:'.red);
};

module.exports.usage = ['Serve this app locally'];